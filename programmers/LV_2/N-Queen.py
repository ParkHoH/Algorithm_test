from copy import deepcopy

def dfs(x, y, copy_graph):


def solution(n):
    graph = [[True] * n for _ in range(n)]
    for i in range(n):
        for j in range(n):
            if graph[i][j]:





print(solution(4))